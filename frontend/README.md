# GlowCoach Frontend

Deploy this folder to Vercel.

Before deploy, open `script.js` and set:

```js
const API_BASE_URL = "https://your-render-backend.onrender.com";
```

Vercel settings:
- Framework Preset: Other
- Root Directory: frontend
- Build Command: leave blank
- Output Directory: ./
